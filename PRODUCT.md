# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

天津工业大学师生，三类身份共用同一个界面：

- **本科生**：自习找位。课间或晚自习时掏手机临时查"哪间空着"，停留时间短、目标明确。
- **研究生 / 考研党**：长时间占座。看重"能不能连着坐几小时"和教室大小。
- **教师 / 教务人员**：找空教室补课、开会、组织活动。

三类的共同使用条件：移动端为主、临时起意、常在走廊或楼梯口单手操作、网络条件不稳定。

## Product Purpose

让师生在到达教学楼之前或当时，快速知道哪间教室此刻是空的。

成功不是"访问量大"，而是**用户看一眼就能走** —— 从打开页面到锁定一间教室的路径越短越好。由于数据来自教务系统而非人工维护，产品的存在价值建立在"这份数据是真的"之上；一旦用户对数据真实性产生怀疑，产品即失去意义。

## Positioning

- 数据直接来自教务系统**当日的真实空教室接口**，每日自动抓取一次，不是人工录入、不是估算、不是课表反推。
- 每条数据带 `source`（live / stale）与 `data_date` 标记。**抓取失败时明说"非实时"，并保留上一次真实数据，绝不生成任何假数据** —— 这是产品的立足点，也是对一次假数据事故的回应（见 `docs/代码审查报告.md`）。
- 无账号、无登录、无后端、无埋点：打开即用，不索取任何个人信息。

## Operating Context

- **使用地点**：教学楼走廊、楼梯口、楼下，常为站立状态、单手持机、户外或室内强光。
- **使用时间**：数据每日北京时间 06:00 更新，覆盖当天全部教学时段；课间（10 分钟）是典型使用窗口，决定了对"快"的要求。
- **时间单位**：教务系统的"小节"映射为五大节（第 1–2 节、第 3–4 节、第 5–6 节、第 7–8 节、第 9–10 节）；已过去的节次依然返回数据。
- **空间单位**：区域 → 楼栋 → 楼层 → 房号。第一公教、第二公教各自只有一栋；**北区含两栋（教学B / 教学C），
  进入北区后才在二级选择具体楼栋**。房号本身编码了楼层（首位数字），因此楼层可由房号推导，无需额外数据。
- **非官方性质**：学生自建工具，与学校教务系统无隶属关系，也不得暗示有。

## Capabilities and Constraints

已确认的能力：

- 每日 06:00（Asia/Shanghai）由 GitHub Actions 登录教务系统抓取，写 `today.json`，GitHub Pages 静态托管。
- 覆盖**第一公共教学楼 + 第二公共教学楼 + 北区两栋（教学B / 教学C）**。
  首次真实抓取（2026-09-16，工作日 16:40）共 **192 间**：
  第一公教 89、第二公教 82、教学B 16、教学C 5。
- **192 是某次抓取的快照，不是固定值**。全天五大节都被占用的教室，接口不会返回
  （同日实测：第一公教"全部教室"92 间，其中 3 间全天占满、未出现在任何小节返回里）。
  因此总数会随日期浮动 —— 这是数据源的真实行为，不是缺漏。
- 每间教室提供房号、座位数、各节次空闲状态（位掩码）。

已确认的约束：

- **覆盖范围固定为两栋公教 + 北区两栋，且明确不再新增** —— 不追求覆盖教务系统返回的全部 17 栋楼。
  理由：数据每天只抓一次，楼栋越多请求越多（每栋 1 次切换 + 5 次大节查询，现 4 栋 ≈ 33 次/天），
  越接近教务系统风控的边界。实训A / 实训B 曾纳入后剔除，因实测各仅 1 间空闲教室，收益不抵成本。
- 教室"类型"字段当前**全部为空**，不可用于筛选或展示，任何依赖它的设计都必须先解决数据缺失。
- **铁律：绝不生成假数据。** 抓取失败必须非零退出、保留上次真实数据、前端显示"非实时"横幅。生产环境禁用 mock 数据生成器。
- 纯静态站：无后端、无数据库、无服务端渲染，所有能力必须在静态文件约束内实现。
- 凭据（学号/密码）只走 GitHub Secrets，不进入任何文件、提交或文档。
- PWA 外壳可用：`manifest.json` + 真 PNG 图标（含 maskable）+ `sw.js`（同源 **network-first**，预缓存逐个 `cache.add`，
  清单内 9 个文件均实际存在）。已支持「添加到主屏幕」引导（Android / iOS Safari / 微信内三种场景）。
- 另提供 Android APK（WebView 直壳，不依赖 Chrome 与 Google 服务），站内自托管下载，见 `docs/ANDROID-APP.md`。
  壳加载的就是本站，**改网页不需要重新出包**。

尚未决定的事实（**不要替它编造答案**）：

- 是否需要"明天"的查询（教务系统存在 `tomrrowDate` 接口，当前未实现，未确认是否有需求）。
- 是否需要收藏、常用教室、历史趋势等留存型功能。
- 真实使用量、用户反馈、设备分布 —— **目前一项都没有**。

## Brand Commitments

- 名称：天津工业大学空教室查询（非官方）。
- 语气：直接、朴素、不营销。数据有问题就说有问题，不用修辞掩盖。
- 不得使用校徽、不得声称官方或权威、不得暗示与教务处有关联。
- 数据权威归于教务系统；本产品只负责如实呈现。

## Evidence on Hand

仓库内真实材料：

- `docs/PRD-修复计划.md`（v1.3）—— 登录协议、接口字段、加密方式、校区编号等逆向结论的真源；
  含逐阶段完工验收与静默测试期安排。
- `docs/api-schema.md` —— 教务系统接口真源（端点、表单字段、响应结构、已知坑）。
- `docs/代码审查报告.md` —— 假数据事故复盘，铁律的来源（文末有逐项修复闭环对照）。
- `docs/ANDROID-APP.md` —— Android 打包与分发（含 TWA 弃用原因）。
- `README.md` —— 运行手册：本地运行、环境变量、验证码重试、故障排查。
- `public/data/today.json`、`web/data/today.json` —— 真实抓取产物（`source=live`，教室数随日期浮动）。
- `.github/workflows/` —— `daily-crawler.yml`（06:00 抓取发布）、`web-healthcheck.yml`（07:00 线上体检）、
  `deploy-pages.yml`（前端改动即发布）、`build-apk.yml`（云端构建 APK）。

**当前状态**：数据链路、前端、分发全部完工（2026-09-13）；自 2026-09-13 起进入**静默测试期**（代码冻结），
考核窗口 **2026-09-14 → 2026-09-20**，目标为连续 7 天 `source=live` 且无人工干预
（09-13 当天含手动触发运行，故不计入窗口，只作冻结起始日）。

**缺失且不得虚构**：用户量、留存、反馈、可用性测试、设备与浏览器分布、竞品对比数据。

## Product Principles

1. **真实优先于完整。** 宁可少显示、显示"数据非实时"，也不补全一份看起来更漂亮但不可信的数据。
2. **先定位空间，再看时间。** 用户的心智路径是"那栋楼那层有没有"，而不是"第几节有空"；界面结构必须服从这个顺序。
3. **一屏之内给出答案。** 典型使用场景是课间十分钟、站着单手操作；需要滚动多屏或多次点击才算失败。
4. **失败必须可见。** 抓取失败、数据过期、网络异常都要在首屏说清楚，让用户自己判断要不要信。
5. **维护负担要小。** 一个人兼职维护的静态站，任何设计决策都要考虑"半年后还能不能跑"。

## Accessibility & Inclusion

产品级要求（以下为 2026-09-13 实测现状，非估计）：

- **文本对比度满足 WCAG AA**：✅ 两套主题、15 组实际配色对全部 ≥4.5:1，
  最低为浅色主题的绿字/绿底 4.93:1；正文最低 16.29:1。
- **正文不小于 14px**：✅ 正文字号 15px。另有 12px 仅用于辅助性标签
  （节次时间、计数、图例、座位数、字段名），属非正文；如需更高可读性可整体上调。
- **触控目标不小于 44×44px**：⚠️ **部分达标**。
  达标：节次芯片 48px、楼栋分段 48px、搜索框 48px、下拉 44px、链接按钮 44px、主按钮 44px；
  未达标：容量筛选 chip 36px、主题切换按钮 40px、安装按钮 40px、显示开关 40px。
  **这是当前唯一未达标的产品级要求**，处置见 `docs/PRD-修复计划.md` §10.4。
- **允许双指缩放**：✅ viewport 无 `user-scalable=no`。
- **状态不能只靠颜色传达**：✅ 空闲格为实心填充、占用格为空心带边框；
  有课的教室卡片用虚线边框并弱化房号；楼层空闲数以数字文本呈现；
  教室的五段日条带 `role="img"` 与"第N大节空闲/有课"的 `aria-label`。
- **户外强光下可读**：走廊与室外场景的硬要求，**尚未在真机户外验证**（不得声称已达标）。
