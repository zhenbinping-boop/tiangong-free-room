# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# 本地凭据（严禁入库；CI 请用 GitHub Secrets）
.env
.env.*
cookies.json

# 抓包样本（体积大，仅本地调试用）
crawler/fixtures/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Android 签名密钥（丢失即无法再给同一批用户发更新，严禁入库）
android/*.keystore
android/*.jks
# 密钥密码等本机说明同样不入库
android/release.keystore*

# 临时产物
.keystore-fingerprint.tmp
